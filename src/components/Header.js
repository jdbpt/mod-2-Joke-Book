import React from 'react';

const Header = (props) => {
  return (
    <header>
        <h1>{props.title}</h1>
        <h3>Where the Jokes Never Run Dry...Get it?</h3>
    </header>
  );
};

export default Header;

